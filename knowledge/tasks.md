# Открытые задачи

Живой список задач, которые нельзя потерять. Закрытые — вычёркивать с датой и коммитом.

> **Порядок работы (Александр, 24.09.2026):** строго поэтапно. Сейчас — только
> **Услуга 1, анимированное поздравление**, пока она не станет идеальной. Потом песня,
> потом клип. Задачи по песне/клипу не начинать, только записывать.

---

## ФЛОУ-1. Оплата ДО начала работы — утверждённый порядок сценария

Решение: Александр, 24.09.2026. Приоритет: P0. **Отменяет** прежнюю схему, где текст
генерировался до оплаты, а платили в самом конце. Для Услуги 1 реализовано 24.09 (см. «Что сделать»).

**Утверждённый порядок:**
1. Приветствие + рассказ о сервисе и услугах.
2. Вопрос «С чего начнём?» → выбор из **трёх** услуг:
   1. Анимированное поздравление (оживлённое фото читает поздравление);
   2. Создание поздравительной песни;
   3. Создание поздравительного клипа.
3. После выбора услуги — рассказ о стоимости и вариантах покупки: **разовая** или **пакет**.
4. Клиент выбирает вариант покупки → кнопки выбора способа оплаты → оплата
   (способы оплаты — см. ОПЛАТА-1: СБП / банки).
5. **Только после оплаты** начинаем работу: повод, текст/стихи, фото, голос и т.д. → генерация.

**Следствия для кода и правил:**
- Сейчас в боте 2 услуги («Анимированное видео» и «Именная песня + клип») — нужно 3,
  песня и клип раздельно.
- Генерация текста теперь идёт после оплаты → переделки текста и отказ стоят клиенту денег.
  2 варианта → 1 переделка (ещё 2) → не подошло → свой текст клиента или отказ
  (с 25.09 — скидка на следующую покупку, см. ниже).
- Email для чека нужен до оплаты — спрашивать на шаге оплаты, не в конце.
- Пакеты = предоплаченные генерации на счету клиента → нужно хранилище (см. ДАННЫЕ-1).
- Приветствие в `src/bot.js` сейчас описывает старый порядок («1. Выбираешь формат и повод
  2. Присылаешь фото и текст 3. Оплачиваешь») — переписать.

**Референс — SoNata (конкурент, скриншоты 24.09.2026):** баллы = внутренняя валюта,
«пакет» = покупка баллов оптом, услуга стоит N баллов (песня = 10 баллов за 2 варианта трека).
Тарифы: 115 ₽ → 10 б. (115 ₽/песня) · 285 ₽ → 40 б. (~71 ₽) · 500 ₽ → 100 б. (50 ₽) ·
1000 ₽ → 250 б. (40 ₽) · 2500 ₽ → 800 б. (~31 ₽). Скидка за объём до ~73%.
Плюс: оплата Telegram Stars, реферальная программа (баллы за друзей), −10% за подписку на канал.
Модель закрывает сразу «разово/пакет», «что такое балл» и «возврат баллами».

**Открытые вопросы к Александру:**
- [ ] Цены: разовая покупка каждой из трёх услуг.
- [ ] Пакеты: что входит (N штук одной услуги? микс услуг?), цена, срок действия.
- [x] Состав услуг (Александр, 24.09.2026):
  - **Анимированное поздравление** — самостоятельная услуга;
  - **Поздравительная песня** — самостоятельная услуга;
  - **Поздравительный клип** — всегда включает создание песни (без песни клипа нет): песня + видео.
  Следствие: клип дороже песни; сценарий клипа = сценарий песни + шаги видео (переиспользовать).
  Возможен апсейл «уже есть песня → доплатить за клип» — обсудить.
- [ ] Клип — какой видеоряд (как у BroHit — только мультяшный? из фото клиента?).
- [x] **Отказ = промокод, не возврат** (Александр, 25.09.2026). Не понравилось (текст или
  готовое видео) → деньги не возвращаем, баллы не начисляем, клиент получает скидку на
  следующую покупку **той же услуги**. Сейчас: **50%, 30 дней** (предварительно — пересмотреть
  после расчёта себестоимости). Для разных услуг скидка может быть разной (`PROMO_RULES` в
  `src/utils/promoStore.js`). Скидка привязана к Telegram-аккаунту, применяется сама, одна
  покупка. Юр. нюанс остаётся: по ЗоЗПП клиент вправе требовать деньги — описать в оферте.
- [~] **Бесплатная проба для новичков** (Александр, 25.09.2026):
  - [x] Дарим **по одному разу бесплатно** анимированное поздравление и песню; клип — после
    расчёта себестоимости.
  - [x] Клиента узнаём по **Telegram ID** (номер телефона пока не просим). Второй визит —
    бот видит, что проба уже была, и повторно её не даёт.
  - [x] **Источник** клиента обязательно фиксируем (маркетинг + будущая реферальная
    программа): ссылки `t.me/<бот>?start=<метка>` — своя метка на каждую рекламу/блогера,
    `ref_<ID клиента>` — рекомендация. Сделано 25.09: `src/utils/userStore.js` (первый визит,
    источник, кто порекомендовал, отметки о пробах), запись при /start.
  - [x] Что дарим (Александр, 25.09): **облегчённый вариант — без переделок** (2 варианта
    текста, «Переделать» нет; отказа со скидкой нет — клиент ничего не платил).
  - [x] (25.09) Подключено в Услугу 1: кнопка «🎁 Попробовать бесплатно» на экране покупки,
    только если проба ещё не использована; отметка ставится сразу при создании заказа.
  - [ ] Когда будет логотип (БРЕНД-1) — ставить его на пробную версию обязательно.
  - Известный обход: второй аккаунт Telegram. Если начнут злоупотреблять — просить номер
    телефона (кнопка «Поделиться номером», одна проба на номер; согласие + 152-ФЗ).
  - Без постоянной базы (ДАННЫЕ-1) всё это обнуляется при деплое → база теперь обязательна.
- [~] Баллы (Александр, 24.09.2026, предварительно, обсуждается; **возвраты баллами отменены
  25.09 — см. промокод выше**): **1 балл = 1 ₽**, баллы
  **бессрочные**; счета скорее раздельные по услугам, т.к. пакеты у трёх услуг будут разные.
  Предложение агента (ждёт решения): разделить два понятия —
  **пакет = предоплаченные штуки конкретной услуги** («3 поздравления за X ₽», счёт у каждой
  услуги свой), **баллы = один общий рублёвый кошелёк** (возвраты «половина баллами», бонусы),
  тратится на любую услугу. Так скидка пакета не ломает 1 балл = 1 ₽.
  Юр. нюанс для оферты: по ЗоЗПП клиент вправе требовать возврат денег, а не баллов, и
  отказаться от неиспользованной предоплаты — описать в оферте, уточнить у юриста.

**Что сделать (после ответов):**
1. [x] (24.09) Новое приветствие и меню из 3 услуг (песня и клип — «скоро», всплывающая подсказка).
2. [~] (24.09) Экран стоимости Услуги 1 — только разовая покупка, 10 ₽ для теста (решение Александра). Пакеты — после расчёта себестоимости.
3. [x] (24.09) Оплата в начале (src/scenes/greetingPurchase.js) → «Оплата получена» + кнопка «Начать» → сценарий → генерация. Брошенный оплаченный заказ продолжается по /start.
4. [x] (24.09) Текст: 2 варианта + 1 переделка (ещё 2, выбор из всех 4) → свой текст или отказ с возвратом половины баллами. Исправлен баг: возврат можно было нажимать многократно.
5. [x] (25.09) Отказ → скидка 50% на следующее поздравление вместо баллов (`greetingDecline.js`,
   `promoStore.js`); экран покупки сам показывает и применяет скидку, гасится после оплаты.
   Команда /balance убрана.
6. [ ] Оплата баллами / списание из пакета (`spendPoints` в balanceStore.js уже есть, не вызывается).

---

## ФОТО-1. Групповое фото — выбрать на нём нужного человека

Поставлено: Александр, 25.09.2026 (живой тест). Приоритет: P1.

Сейчас фото, где кроме поздравляемого есть кто-то ещё, бот не принимает и просит другое.
Идея: клиент присылает общее фото → бот сам находит лица, подписывает их номерами →
клиент нажимает номер нужного человека → бот вырезает его → показывает «Так?» → дальше.
Заодно снимается этический вопрос: посторонних в кадре не остаётся, вопрос «есть ли на
фото кто-то ещё» можно убрать. Риск: вырезанный из общего фото кусок может быть мелким —
видео выйдет нечётким; при слишком маленьком лице честно просить другое фото.
- [ ] Решение Александра: делать автоматический выбор по номеру (см. выше) или пока проще —
  подсказка «обрежьте фото встроенным редактором Telegram перед отправкой».
- [ ] Выбрать способ поиска лиц (библиотека в боте или облачный сервис) и оценить стоимость.

---

## БРЕНД-1. Логотип и музыкальная подпись на всех наших работах

Поставлено: Александр, 25.09.2026. Приоритет: P1.

- [x] Способ (Александр, 25.09.2026): **генерируем ИИ**.
- [x] Логотип (Александр, 25.09.2026): вариант №4 — подарок с кнопкой «play». Файлы в `knowledge/brand/final/`:
  `logo-ru.png` («Поздрав Сервис»), `logo-en.png` («Pozdrav Servis», по просьбе Александра),
  `icon.png` — значок без текста, прозрачный фон (аватарка бота, метка на видео), `icon-on-white.png` — он же на белом.
  Черновики — `knowledge/brand/logo-candidates/` (у 1–3 и 7 фон прозрачный; тёмное «свечение» — артефакт просмотрщика).
- [~] (25.09) Аватарка бота поставлена — логотип с надписью (мелко в кружке). Рекомендовано заменить на
  `icon-on-white.png` (BotFather → /setuserpic).
- [ ] Имя бота **@TITAL_NAPAMYAT_bot** не совпадает с брендом PozdravServis — решить до рекламы
  (ссылки t.me/<бот>?start=… будут с этим именем). Варианты: новый бот с именем под бренд и перенос токена/настроек.
- [~] Звуковой логотип (25.09): Александр хочет, чтобы голосом звучало название. 6 вариантов по 5 с в
  `knowledge/brand/sound-candidates/`: `logo-sound-{female,male}-a-bells-{1,2}` — ведущий (OpenAI TTS)
  «ПоздравСервис!» поверх мелодии с колокольчиками (Mureka instrumental); `logo-sound-sung-{1,2}` —
  название спето (Mureka song). Сырые треки в `raw/` (не в git).
  **Выбран `logo-sound-sung-1`** (Александр, 25.09). Добавить слоган **«Креативный подарок по любому поводу»** (уточнено Александром) —
  варианты: всё спето заново (Mureka) или sung-1 + слоган голосом ведущей.
  25.09: слоган голосом ведущей (OpenAI TTS) Александру не понравился — не сочетается с певицей.
  Новые: `logo-sound-slogan-sung-{1,2}` — одна певица поёт и название, и слоган (Mureka, трек raw/slogan-a-2).
  Голос OpenAI для слогана снова не понравился; Александр хотел «как Алиса» — отказано копировать голос
  конкретной актрисы/бренда Яндекса (права на голос, риск ассоциации с Яндексом). Вместо — официальные
  голоса Яндекс SpeechKit (alena, jane, marina): `logo-sound-sung-1-slogan-yandex-*`. Ключ — YANDEX_GPT_API_KEY
  из проекта «День рождения».
- [ ] Прикреплять логотип и звук ко всем готовым продуктам (видео, песня, клип).
- [ ] Позже: настройка у клиента — отключить наш идентификатор на своих заказах
  (обсудить: для всех или только в платной версии / за доплату; в пробной — всегда с логотипом).

---

## HEYGEN-1. Лимит клонов голоса (2 на тарифе)

Живой тест 25.09.2026: «resource_limit_reached» и после чистки — бот удаляет только свои клоны
`client-<время>` старше 30 мин, а слоты, видимо, заняты голосами, созданными вручную в кабинете HeyGen.
- [x] В журнал пишется список голосов, которые видит бот («HeyGen private voices: …»).
- [x] Клиенту при нехватке слотов — кнопка «🔊 Сделать стандартным голосом» (+ «Попробовать ещё раз»).
- [x] Уведомление владельцу о сбое в Telegram — переменная Railway `ADMIN_CHAT_ID` (свой ID — команда /id в боте). Добавлена в Railway 25.09 13:15.
- [x] (25.09 12:55) Александр удалил 2 старых тестовых клона (test-wife-voice, test-wife-voice-2) — оба слота свободны.
- [ ] Решить: одновременно голосом клиента — не больше 2 заказов (тариф). Нужен ли тариф выше?
- [ ] Качество видео (Avatar IV, docs developers.heygen.com/avatar-iv): попробовать `expressiveness`
  (low по умолчанию → medium/high) и `motion_prompt` («улыбается, слегка кивает») — сравнить на тестах.

---

## ДЕПЛОЙ-1. Перезапуск бота обрывал генерацию видео

25.09.2026 13:18–13:34: клиент (Александр) ждал видео 16 минут — ни видео, ни сообщения о сбое.
Причина: каждый push (даже правка только knowledge/tasks.md) перезапускал бота на Railway, а
генерация живёт в памяти процесса — заказ навсегда оставался в статусе generating.
- [x] `railway.json` → `build.watchPatterns`: деплой только при изменениях src/, certs/, package*.json.
- [x] При старте бот подхватывает заказы в статусе generating (до 2 раз, потом — сбой с кнопкой повтора).
- [ ] Проверить после деплоя, что правка только в knowledge/ больше не вызывает деплой.

---

## ДАННЫЕ-1. Постоянное хранилище: клиенты, заказы, баллы, пакеты

Поставлено: 24.09.2026. Приоритет: P0 (без него баллы и пакеты могут пропасть).

- Сейчас заказы и баллы — JSON-файлы в `data/` на Railway. Если у сервиса нет Volume,
  файлы **стираются при каждом деплое**. Проверить в Railway → сервис → Volumes.
  **Проверено 25.09.2026 (скриншот Александра): Volume `na-pamyat-d-bot-volume` подключён,
  Mount path `/app/data` — совпадает с папкой `data/` бота, данные переживают деплой.**
  500 МБ, регион — Запад США (для 152-ФЗ всё равно нужна БД в РФ до реальных клиентов).
  С 25.09 там же лежат промокоды (`data/promos.json`).
- Кандидаты: Supabase (предлагал коллега; удобная веб-таблица для Александра, free tier засыпает
  через неделю без активности) или PostgreSQL в Railway.
- **152-ФЗ:** персональные данные граждан РФ (email, фото, Telegram ID) должны изначально
  записываться в БД на территории РФ. Supabase/Railway — за рубежом. Для тестов не критично,
  до реальных клиентов — решить. Российские варианты: Yandex Cloud Managed PostgreSQL
  (аккаунт у Александра есть), Selectel, Timeweb. Уточнить у юриста/бухгалтера.
- [x] Решение по базе (Александр, 25.09.2026): **российская PostgreSQL** (152-ФЗ). Рекомендована
  **Timeweb Cloud** (от ~230 ₽/мес, проще настройка); альтернатива — Yandex Cloud (аккаунт есть).
- [x] (25.09) Александр создал базу: Timeweb Cloud, PostgreSQL 18, Москва, 1 CPU / 1 ГБ / 8 ГБ,
  публичный IPv4, бэкапы раз в день — **744 ₽/мес** (не 230, как оценивалось: +200 IPv4, +48 бэкапы).
  База `default_db`, пользователь `gen_user`. В Railway: `DATABASE_URL` (без пароля) +
  `DATABASE_PASSWORD`. Баланс 500 ₽ — до ~17.10, потом пополнить.
- [x] (25.09) Агент: `src/utils/docStore.js` — таблицы orders / users / promos (id, user_id,
  status, data jsonb), записи в памяти + запись каждого изменения в базу; разовый перенос из
  `data/*.json` (файлы оставлены как резервная копия); SSL с сертификатом Timeweb
  (`certs/timeweb-ca.crt`). Проверено на локальной PostgreSQL 18.
- [x] (25.09 12:28) Деплой a738773: в логах Railway «перенесено в orders: 2», «users: 1», «PostgreSQL подключена» — работает.
- [ ] Когда процессов станет больше одного — убрать кэш в памяти, читать из базы напрямую.
- [ ] Удалить неиспользуемый `balanceStore.js` (баллы заменены промокодом) или перенести в БД,
  если вернёмся к баллам/пакетам.
- [ ] Схема: клиенты, заказы (что заказано/получено), баланс баллов + история операций, пакеты.
- [ ] Перенос orderStore.js / balanceStore.js на БД.

---

## ОПЛАТА-1. Оплата по СБП с выбором банка (вместо текущей ссылки ЮKassa)

Поставлено: Александр, 24.09.2026. Приоритет: P0 (без рабочей оплаты нет запуска).

**Как хочет Александр** (скриншот от 24.09): страница оплаты как у qr.nspk.ru —
сумма и название услуги сверху, ниже «Выберите способ оплаты»: плитки двух самых
используемых банков (Сбербанк, Т-Банк), под ними «Система быстрых платежей» (полный
список банков). Человек нажимает банк — открывается приложение банка — подтверждает.
Без ввода карты.

**Почему текущая схема плохая** (живой прогон 23–24.09.2026):
- бот присылает длинную голую ссылку на yoomoney.ru вместо кнопки;
- ссылка живёт ~1 час, потом «Time has run out» — нужно проходить весь сценарий заново;
- после оплаты кнопка «Back to the website» ведёт на `https://t.me/TITAL_NAPAMYAT_bot`,
  а t.me в браузере в РФ часто не открывается без VPN → ERR_CONNECTION_TIMED_OUT.

**Почему оплата 23.09 не прошла:** магазин тестовый, а в форму ввели настоящую карту МИР →
«It didn't work: technical error». Тестовый магазин принимает только тестовые карты ЮKassa
(например `5555 5555 5555 4477`, срок любой в будущем, CVC любой) — реальные деньги не спишутся.

**Что проверено:**
- ЮKassa поддерживает СБП: `payment_method_data: { type: "sbp" }` + `confirmation.type: "redirect"`,
  ссылка ведёт на страницу выбора банка.
- 24.09.2026 пробный СБП-платёж на текущих (тестовых) ключах PozdravServis →
  `400 invalid_request: Payment method is not available`. В тестовом магазине СБП нет.
- Плитки «Вы ранее использовали» (Сбер, Т-Банк) на странице НСПК рисует сама НСПК
  по истории оплат человека — мы ими не управляем. У нового клиента их может не быть,
  тогда он увидит общий пункт «Система быстрых платежей». Проверить на боевом магазине.

**Что сделать:**
1. [ ] Боевой магазин ЮKassa для PozdravServis (у Александра есть боевой для другого
   сервиса — но чеки/договор привязаны к магазину, поэтому для PozdravServis нужен свой
   магазин или отдельный магазин в том же кабинете). Подключить в нём СБП.
2. [ ] В `createPayment` передавать `payment_method_data: { type: "sbp" }`, когда СБП
   доступен; если ЮKassa отвечает «Payment method is not available» — откатываться на
   обычную страницу ЮKassa, чтобы оплата не ломалась.
3. [x] (24.09) Кнопка «💳 Оплатить N ₽» (inline-кнопка с url) вместо голой ссылки в тексте.
4. [ ] Кнопка «Получить новую ссылку», если платёж истёк/отменён — без повторного прохождения сценария.
5. [x] (24.09, коммит «сообщение об оплате») После вебхука об оплате бот сам пишет в чат «Оплата получена, генерация началась, до 10 минут» —
   не полагаемся на возврат через t.me.
6. [ ] `return_url` — не t.me (блокируется в браузере). Варианты: `tg://resolve?domain=<bot>`
   или своя простая страница «Оплата прошла, вернитесь в Telegram» на домене бота.
7. [ ] Проверить на боевом магазине с реальной оплатой 10 ₽: СБП → банк → вебхук → генерация.

---

## ТЕКСТ-1. Качество стихов и ограничение длины (из прогона 23.09.2026)

**Сделано 24.09.2026 (временное решение, будем тестировать ещё):**
- [x] Слепое сравнение на вводных прогона 23.09 (Новый год, друг 37 лет, рыбалка, стройматериалы,
  Елена, Григорий): gpt-4o-mini / gpt-5.4-mini / gpt-5.5 / YandexGPT (ключ из проекта «День рождения»),
  по 2 стиха. Александр выбрал 3: два от gpt-5.5, один от gpt-5.4-mini. YandexGPT и gpt-4o-mini — ни одного.
- [x] Модель генерации текста: gpt-4o-mini → gpt-5.5 (`reasoning_effort: low`), ~3–4 ₽ за 2 варианта (оценка).
- [x] Длина: варианты длиннее 75 слов перегенерируются (до 2 попыток, берётся более короткий).
- [ ] Повторить сравнение позже (другие поводы, проза; возможно Claude — нужен ключ Anthropic).
- [ ] Вписать реальную стоимость текста в knowledge/unit-economics.md после боевого прогона.

Исходные находки:
- Стихи слабые: «друг мой dear», «лишь меры», «удачи эффект», «Пусть мечтаета ждет!» —
  англицизмы, битая рифма, несуществующие слова. Нужна доработка промпта генерации стихов.
- Вариант 2 заметно длиннее ограничения ~75 слов — ограничение не соблюдается.
- Проверить, что текст соответствует выбранному поводу (в прогоне был «С Новым годом» — уточнить, какой повод выбирался).
