# Открытые задачи

Живой список задач, которые нельзя потерять. Закрытые — вычёркивать с датой и коммитом.

---

## ОПЛАТА-1. Оплата по СБП с выбором банка (вместо текущей ссылки ЮKassa)

Поставлено: Александр, 24.09.2026. Приоритет: P0 (без рабочей оплаты нет запуска).

**Как хочет Александр** (скриншот от 24.09): страница оплаты как у qr.nspk.ru —
сумма и название услуги сверху, ниже «Выберите способ оплаты»: плитки двух самых
используемых банков (Сбербанк, Т-Банк), под ними «Система быстрых платежей» (полный
список банков). Человек нажимает банк — открывается приложение банка — подтверждает.
Без ввода карты.

**Почему текущая схема плохая** (живой прогон 23–24.09.2026):
- бот присылает длинную голую ссылку на yoomoney.ru вместо кнопки;
- ссылка живёт ~1 час, потом «Time has run out» — нужно проходить весь сценарий заново;
- после оплаты кнопка «Back to the website» ведёт на `https://t.me/TITAL_NAPAMYAT_bot`,
  а t.me в браузере в РФ часто не открывается без VPN → ERR_CONNECTION_TIMED_OUT.

**Что проверено:**
- ЮKassa поддерживает СБП: `payment_method_data: { type: "sbp" }` + `confirmation.type: "redirect"`,
  ссылка ведёт на страницу выбора банка.
- 24.09.2026 пробный СБП-платёж на текущих (тестовых) ключах PozdravServis →
  `400 invalid_request: Payment method is not available`. В тестовом магазине СБП нет.
- Плитки «Вы ранее использовали» (Сбер, Т-Банк) на странице НСПК рисует сама НСПК
  по истории оплат человека — мы ими не управляем. У нового клиента их может не быть,
  тогда он увидит общий пункт «Система быстрых платежей». Проверить на боевом магазине.

**Что сделать:**
1. [ ] Боевой магазин ЮKassa для PozdravServis (у Александра есть боевой для другого
   сервиса — но чеки/договор привязаны к магазину, поэтому для PozdravServis нужен свой
   магазин или отдельный магазин в том же кабинете). Подключить в нём СБП.
2. [ ] В `createPayment` передавать `payment_method_data: { type: "sbp" }`, когда СБП
   доступен; если ЮKassa отвечает «Payment method is not available» — откатываться на
   обычную страницу ЮKassa, чтобы оплата не ломалась.
3. [ ] Кнопка «💳 Оплатить N ₽» (inline-кнопка с url) вместо голой ссылки в тексте.
4. [ ] Кнопка «Получить новую ссылку», если платёж истёк/отменён — без повторного прохождения сценария.
5. [ ] После вебхука об оплате бот сам пишет в чат «Оплата получена, делаю видео» —
   не полагаемся на возврат через t.me.
6. [ ] `return_url` — не t.me (блокируется в браузере). Варианты: `tg://resolve?domain=<bot>`
   или своя простая страница «Оплата прошла, вернитесь в Telegram» на домене бота.
7. [ ] Проверить на боевом магазине с реальной оплатой 10 ₽: СБП → банк → вебхук → генерация.

---

## ТЕКСТ-1. Качество стихов и ограничение длины (из прогона 23.09.2026)

- Стихи слабые: «друг мой dear», «лишь меры», «удачи эффект», «Пусть мечтаета ждет!» —
  англицизмы, битая рифма, несуществующие слова. Нужна доработка промпта генерации стихов.
- Вариант 2 заметно длиннее ограничения ~75 слов — ограничение не соблюдается.
- Проверить, что текст соответствует выбранному поводу (в прогоне был «С Новым годом» — уточнить, какой повод выбирался).
